{
  "family": "ecommerce-storefront",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "executionRoleArn": "arn:aws:iam::900930545753:role/ecommerceTaskExecutionRole",
  "containerDefinitions": [
    {
      "name": "storefront",
      "image": "900930545753.dkr.ecr.us-east-1.amazonaws.com/ecommerce-storefront:latest",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 3000,
          "protocol": "tcp"
        }
      ],
      "environment": [
        { "name": "PORT", "value": "3000" },
        { "name": "API_URL", "value": "http://store.ecommerce.local:5000" }
      ],
      "healthCheck": {
        "command": ["CMD-SHELL", "wget -qO- http://localhost:3000/ >/dev/null || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 30
      },
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/ecommerce-storefront",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "storefront",
          "awslogs-create-group": "true"
        }
      }
    }
  ]
}
